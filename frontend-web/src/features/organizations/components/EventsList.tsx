import type { Organization } from '../../../services/api'
export function EventsList({ organization }: { organization: Organization }) { return <section className="events-card"><div className="section-heading"><div><span className="eyebrow">PREHĽAD</span><h2>Vytvorené eventy</h2></div><span className="event-count">{organization.events.length}</span></div>{organization.events.length === 0 ? <div className="empty"><h3>Zatiaľ bez eventov</h3><p>Prvý event vytvoríš formulárom vedľa.</p></div> : <div className="event-list">{organization.events.map((item) => <article className="event-item" key={item.id}><div className="event-icon">{item.sport.charAt(0).toUpperCase()}</div><div><h3>{item.name}</h3><p className="event-meta">{item.sport} · {item.participation_type === 'TEAM' ? 'Tímové' : 'Samostatné'}</p><p className="event-meta">{item.event_date ? new Intl.DateTimeFormat('sk-SK', { dateStyle: 'medium' }).format(new Date(`${item.event_date}T12:00:00`)) : 'Termín sa doplní'}{item.location ? ` · ${item.location}` : ''}</p>{item.description && <p className="event-description">{item.description}</p>}</div><strong>{item.fee === null ? 'Zdarma' : `${Number(item.fee).toFixed(2).replace('.', ',')} €`}</strong></article>)}</div>}</section> }
