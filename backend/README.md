# Backend

FastAPI service. Run `docker compose up --build` from the repository root; development API docs are at `http://localhost:8000/docs`. Production disables interactive API documentation and rejects insecure configuration.
